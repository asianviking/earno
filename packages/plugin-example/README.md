# @earno/plugin-example

Example `earno` plugin (demo strategies / wiring).

## Install

```sh
npm i @earno/plugin-example
```

## Use with `earno`

```sh
EARNO_PLUGINS=@earno/plugin-example earno example send 0.01 --to 0xYourAddress --web
```

