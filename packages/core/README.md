# @earno/core

Shared core types + request transport for earno (monorepo: `asianviking/earno`).

This package is primarily used by the `earno` CLI and plugins.

## Install

```sh
npm i @earno/core
```

